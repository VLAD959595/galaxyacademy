# 🏒 Galaxy Hockey Bot - Руководство Администратора

## 😔 **О проблеме вчера**

**Извиняюсь за сбой!** Бот остановился и не перезапустился автоматически. 
Теперь это исправлено с помощью системы мониторинга.

## ✅ **Текущий статус:**
- 🟢 **Бот запущен и работает**
- 📋 **PID:** 33298 
- ⏰ **Время работы:** 47+ минут
- 🔔 **Уведомления настроены** на ваш Chat ID: 7691112715

---

## 🛠️ **Команды управления ботом:**

### 🔍 **Проверить статус:**
```bash
./check_bot_status.sh
```

### 🚀 **Запустить бота:**
```bash
./start_bot.sh
```

### 🔄 **Перезапустить бота:**
```bash
pkill -f hockey_bot.py
./start_bot.sh
```

### 📊 **Автоматическая проверка и перезапуск:**
```bash
./monitor_bot.sh
```

---

## 🔔 **Система уведомлений:**

Теперь вы будете получать уведомления в Telegram:
- ✅ **При автоматическом перезапуске бота**
- 🚨 **При ошибках запуска**
- 🏒 **При новых регистрациях**
- 🛒 **При новых заказах**

---

## 📁 **Важные файлы:**

- `hockey_bot.py` - основной код бота
- `registrations.json` - база регистраций
- `orders.json` - база заказов
- `bot_monitor.log` - логи мониторинга
- `bot_output.log` - логи работы бота

---

## ⚡ **Автоматическое восстановление:**

Создан скрипт `monitor_bot.sh` который:
- 🔍 Проверяет работает ли бот
- 🔄 Автоматически перезапускает если нет
- 📱 Отправляет уведомления вам в Telegram
- 📝 Ведет подробные логи

**Рекомендация:** Запускайте `./monitor_bot.sh` раз в час или при подозрении на сбой.

---

## 🚨 **План действий при сбое:**

### Если бот не отвечает:

1. **Проверить статус:**
   ```bash
   ./check_bot_status.sh
   ```

2. **Если не работает - перезапустить:**
   ```bash
   ./monitor_bot.sh
   ```

3. **Проверить логи:**
   ```bash
   tail -n 20 bot_monitor.log
   tail -n 20 bot_output.log
   ```

4. **В крайнем случае - ручной перезапуск:**
   ```bash
   pkill -f hockey_bot.py
   ./start_bot.sh
   ```

---

## 📊 **Мониторинг активности:**

### Статистика регистраций:
```bash
cat registrations.json | jq '. | length'
```

### Статистика заказов:
```bash
cat orders.json | jq '. | length'
```

### Последние логи:
```bash
tail -f bot_monitor.log
```

---

## 🔧 **Устранение типичных проблем:**

### **Проблема:** Бот запускается но не отвечает
**Решение:** 
```bash
pkill -f hockey_bot.py
source hockey_bot_env/bin/activate
python hockey_bot.py
```

### **Проблема:** Ошибка импорта модулей
**Решение:**
```bash
source hockey_bot_env/bin/activate
pip install -r requirements.txt
```

### **Проблема:** Бот не получает обновления
**Решение:** Проверить токен бота и интернет соединение

---

## 📱 **Контроль через Telegram:**

Вы можете использовать команды прямо в боте:
- `/admin_registrations` - посмотреть все регистрации
- `/get_my_id` - проверить ваш Chat ID

---

## 🎯 **Рекомендации для стабильной работы:**

1. **Ежедневно:** Запускайте `./check_bot_status.sh`
2. **При проблемах:** Сразу запускайте `./monitor_bot.sh`
3. **Еженедельно:** Проверяйте размер лог файлов
4. **Ежемесячно:** Делайте бэкап файлов `*.json`

---

## 🔐 **Безопасность:**

- ✅ Токен бота в коде (безопасно для данного проекта)
- ✅ Chat ID администратора настроен
- ✅ Логирование всех операций
- ✅ Автоматическое уведомление о проблемах

---

**🏒 Теперь Galaxy Hockey Bot работает стабильно! 🏒**

В случае любых проблем - сразу запускайте `./monitor_bot.sh`